# 任务规格

## 任务目标
- Drive the workflow through an external driver

## 业务背景
- 本任务用于搭建一个可复用的 MAS V1 工作区，并为其配上可执行的工作流规则。
该工作区旨在成为后续项目交付的稳定机制。

## 范围
- 让三省六部运行时在 Codex 环境中自动推进。

## 输入输出
- 输入：任务 ID: `runtime-module`
标题: Runtime Module
目标: Drive the workflow through an external driver
皇帝需求: Use the external driver contract instead of the built-in simulator
- 输出：可供后续部门消费的规格与约束。

## 验收标准
- 规格、门禁和人工介入路径清晰。

## 完成检查清单
- [x] 规格完整
- [x] 范围明确
- [x] 约束已记录
